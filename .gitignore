# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
# pubspec.lock is intentionally committed (this is an app, not a library)

# Coverage
coverage/
*.lcov

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude Code (per-developer local state only)
.claude/settings.local.json

# Platform (re-add when `flutter create .` is run)
android/.gradle/
android/.idea/
android/local.properties
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec

# Generated
*.g.dart
*.freezed.dart
.generated/

# Local-only artifacts (AVD screencaps, ad-hoc run logs)
.avd-snapshot*.png
.flutter-run.log
